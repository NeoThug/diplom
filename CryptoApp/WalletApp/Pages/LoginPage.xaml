<Page x:Class="WalletApp.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WalletApp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="400"
      Title="LoginPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0"/>
            <RowDefinition Height="300"/>
            <RowDefinition Height="0"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Введите логин" TextAlignment="Center" Foreground="Beige"/>
            <TextBox Name="TbLogin"/>
            <TextBlock Text="Введите пароль" TextAlignment="Center" Foreground="Beige"/>
            <PasswordBox Name="PbPass"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Войти" Name="SignIn" Click="SignIn_Click" Margin="5"/>
                <Button Content="Регистрация" Name="SignUp" Click="SignUp_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
