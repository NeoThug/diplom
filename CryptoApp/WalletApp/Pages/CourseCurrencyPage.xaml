<Page x:Class="WalletApp.Pages.CourseCurrencyPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WalletApp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CourseCurrencyPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="300"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>


        <DataGrid ItemsSource="{Binding Currency}" AutoGenerateColumns="False" Name="DGCurrency" Grid.Row="1" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Валюта" Binding="{Binding Currency1}" Width="400"/>
                <DataGridTextColumn Header="Стоимость в рублях" Binding="{Binding PriceInRub}" Width="400"/>
            </DataGrid.Columns>
        </DataGrid>
        <TextBlock Text="Курсы валют"/>
        <Button Grid.Row="2" Content="Назад" Name="Back" Click="Back_Click"/>
    </Grid>
</Page>
